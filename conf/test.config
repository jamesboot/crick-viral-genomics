// Small test with generated data

params {
    // Limit resources so that this can run on CI
    max_cpus   = 2
    max_memory = '8.GB'
    max_time   = '6.h'

    // Pipeline params
    debug = true
    generate_reads = true
}
